<a class="brand-logo" href="/" aria-label="Body Reset by Georgia — home">
  <img src="/body-reset-logo.jpg" alt="Body Reset by Georgia" width="720" height="719" />
</a>

<style>
  .brand-logo {
    display: block;
    width: clamp(170px, 19vw, 240px);
    flex-shrink: 0;
    border-radius: 4px;
    overflow: hidden;
    background: white;
  }
  .brand-logo img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 2 / 1;
    object-fit: cover;
    object-position: center;
  }
  .brand-logo:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 4px;
  }
  @media (max-width: 480px) {
    .brand-logo { width: 150px; }
  }
</style>
